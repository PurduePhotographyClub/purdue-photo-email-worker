{
  "name": "purdue-photography-club-email-worker",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --port 8791",
    "test": "node --test --experimental-strip-types src/index.test.mjs",
    "typecheck": "tsc --noEmit",
    "doctor": "npx react-doctor@latest --verbose --no-score --yes",
    "verify": "npm run test && npm run typecheck && npm run build && npm run doctor"
  },
  "reactDoctor": {
    "ignore": {
      "files": ["dist/**", ".wrangler/**"]
    }
  },
  "dependencies": {
    "postal-mime": "^2.6.0",
    "unpdf": "^1.6.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260730.1",
    "typescript": "^6.0.3",
    "wrangler": "^4.115.0"
  }
}
